// - Offer recent draft list to pick-up from
// - or Create new draft
// - if no draft is available, go straight to create new draft
'use strict';

const nkm = require(`@nkmjs/core`);
const polyCore = require(`@polymorse/core`);

class ProfileView extends nkm.ui.views.View {
    constructor() { super(); }

    _Init(){
        super._Init();
    }

    _OnDisplayGain() { 
        
    }

}

module.exports = ProfileView;
