'use strict';

const nkm = require(`@nkmjs/core`);

const base = require(`./card-entity`);
class DraftCard extends base {
    constructor() { super(); }

    _Init() {
        super._Init();
    }

    _CleanUp() {
        super._CleanUp();
    }

}

module.exports = DraftCard;